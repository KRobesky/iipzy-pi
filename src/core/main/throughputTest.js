const {
  init,
  testWindowStart,
  throughputTestEnableWrites,
  validateThroughputTestRrdb
} = require("./throughputTest_impl");
module.exports = {
  init,
  testWindowStart,
  throughputTestEnableWrites,
  validateThroughputTestRrdb
};
